# This module does not require a backend configuration
# Backend should be configured by the consumer of this module at deployment time
terraform {
  # Backend configuration is managed by the consumer of this module
}
